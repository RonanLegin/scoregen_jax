from setuptools import setup, find_packages

setup(
    name='scoregen',
    version='1.0.0',
    packages=find_packages(),
    author='Ronan Legin',
    description='A simple package to train 1D diffuson models in JAX',
)

